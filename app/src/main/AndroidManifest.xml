<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectdidong">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/course_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/course_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GiangVienActivity"></activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PhuHuynhActivity" />
        <activity android:name=".SinhVienActivity" />
    </application>

</manifest>